# Watch-db (SQLite) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cuaderno del relojero — almacenamiento persistente de relojes y sesiones de medición en SQLite (`~/tg-data/tg.db`), con API `watchdb_*` lista para el panel izquierdo (5.5b) y export JSON por reloj vía el módulo `json.c`.

**Architecture:** Módulo `json.c/.h` autónomo (escritor streaming + parser DOM acotado, con tests) usado para `watchdb_export_watch_json()`. Módulo `watchdb.c/.h` sobre SQLite: tablas `watches` y `sessions` (FK con ON DELETE CASCADE), CRUD + consultas, apertura/init de `~/tg-data/tg.db`, y `watchdb_capture_session()` que toma `stats_summary(0)` + config actual en un solo punto. Dependencia nueva: `libsqlite3-dev` / `sqlite-devel` / `mingw-w64-x86_64-sqlite3` (PKG_CHECK_MODULES). Sin UI (esa es la Fase 5.5b).

**Tech Stack:** C99, SQLite3, pthread, autotools `make check`.

**Rama:** `feature/watch-db` (creada desde `master`).

---

## Estructura de archivos

- Create: `src/json.h`, `src/json.c`, `src/watchdb.h`, `src/watchdb.c`, `tests/test_json.c`, `tests/test_watchdb.c`
- Modify: `configure.ac`, `Makefile.am`, `.gitignore`, `src/tg.h` (nada por ahora)
- Docs: `docs/ROADMAP.md`, `README.md`, `docs/DEVELOPMENT.md`

---

### Task 1: `json.c/.h` + tests

**Files:** Create `src/json.h`, `src/json.c`, `tests/test_json.c`; Modify `Makefile.am`, `.gitignore`

- [ ] **Step 1: Escribir el test (falla por falta del módulo)**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
#include "json.h"

static int failures = 0;
#define CHECK(cond, msg) do { if(!(cond)) { fprintf(stderr, "FAIL: %s\n", msg); failures++; } } while(0)

int main(void)
{
	/* --- Escritor --- */
	char buf[4096];
	struct json_writer w;
	jw_init(&w, buf, sizeof(buf));
	jw_object_begin(&w);
	jw_key(&w, "name");
	jw_string(&w, "Omega \"565\"");
	jw_key(&w, "bph");
	jw_int(&w, 21600);
	jw_key(&w, "rate");
	jw_double(&w, 177.556);
	jw_key(&w, "tags");
	jw_array_begin(&w);
	jw_string(&w, "a");
	jw_string(&w, "b");
	jw_array_end(&w);
	jw_key(&w, "ok");
	jw_bool(&w, 1);
	jw_key(&w, "none");
	jw_null(&w);
	jw_object_end(&w);
	CHECK(jw_ok(&w), "writer ok");
	CHECK(strstr(buf, "\"name\":\"Omega \\\"565\\\"\"") != NULL, "escape");
	CHECK(strstr(buf, "\"bph\":21600") != NULL, "int");
	CHECK(strstr(buf, "\"rate\":177.556000") != NULL, "double");
	CHECK(strstr(buf, "\"tags\":[\"a\",\"b\"]") != NULL, "array");
	CHECK(strstr(buf, "\"ok\":true") != NULL && strstr(buf, "\"none\":null") != NULL, "bool/null");

	/* --- Parser (round-trip) --- */
	struct json_value *v = json_parse(buf, strlen(buf));
	CHECK(v != NULL, "parse ok");
	CHECK(v->type == JSON_OBJECT, "root object");
	struct json_value *name = json_obj_get(v, "name");
	CHECK(name && name->type == JSON_STRING && !strcmp(name->u.str, "Omega \"565\""), "name round-trip");
	struct json_value *bph = json_obj_get(v, "bph");
	CHECK(bph && bph->type == JSON_NUMBER && fabs(bph->u.num - 21600) < 1e-9, "bph");
	struct json_value *tags = json_obj_get(v, "tags");
	CHECK(tags && tags->type == JSON_ARRAY && json_arr_size(tags) == 2, "tags array");
	struct json_value *t0 = json_arr_get(tags, 0);
	CHECK(t0 && t0->type == JSON_STRING && !strcmp(t0->u.str, "a"), "tag0");
	CHECK(json_obj_get(v, "missing") == NULL, "missing key");

	json_free(v);

	/* --- Malformado -> error sin crash --- */
	CHECK(json_parse("{", 1) == NULL, "truncated");
	CHECK(json_parse("{\"a\":}", 6) == NULL, "bad value");
	CHECK(json_parse("not json", 8) == NULL, "garbage");
	CHECK(json_parse("{\"a\":1},", 8) == NULL, "trailing");

	if(failures) { fprintf(stderr, "%d failure(s)\n", failures); return 1; }
	printf("json tests passed\n");
	return 0;
}
```

- [ ] **Step 2: Ejecutar para verlo fallar**

```bash
make check 2>&1 | tail -6
```

- [ ] **Step 3: Implementar `src/json.h`** (cabecera GPL-2 como src/wav.h):

```c
#ifndef TG_JSON_H
#define TG_JSON_H

#include <stdio.h>
#include <stddef.h>

/* --- Escritor streaming (a buffer) --- */
struct json_writer {
	char *buf;
	size_t size, pos;
	int err;
};

void jw_init(struct json_writer *w, char *buf, size_t size);
int jw_ok(const struct json_writer *w);          /* 0 = hubo error (buffer lleno) */
void jw_object_begin(struct json_writer *w);
void jw_object_end(struct json_writer *w);
void jw_array_begin(struct json_writer *w);
void jw_array_end(struct json_writer *w);
void jw_key(struct json_writer *w, const char *key);
void jw_string(struct json_writer *w, const char *s);
void jw_int(struct json_writer *w, long long v);
void jw_double(struct json_writer *w, double v);
void jw_bool(struct json_writer *w, int b);
void jw_null(struct json_writer *w);

/* --- Lector DOM --- */
enum json_type { JSON_NULL, JSON_BOOL, JSON_NUMBER, JSON_STRING, JSON_ARRAY, JSON_OBJECT };

struct json_value {
	enum json_type type;
	union {
		int b;
		double num;
		char *str;
		struct { struct json_value **items; int n; } arr;      /* array y objeto (pares key/value en items alternos para objeto) */
	} u;
	char *key;                 /* clave cuando es value de objeto */
	struct json_value *next;   /* siguiente hermano */
};

struct json_value *json_parse(const char *text, size_t len);
void json_free(struct json_value *v);
struct json_value *json_obj_get(struct json_value *obj, const char *key);
int json_arr_size(struct json_value *arr);
struct json_value *json_arr_get(struct json_value *arr, int i);

#endif
```

- [ ] **Step 4: Implementar `src/json.c`** (cabecera GPL-2; escritura con `snprintf` acotado y coma gestionada por profundidad; parser recursivo descendente con límite de profundidad 32; strings con escapado de `"` `\` `\b\f\n\r\t` y `\uXXXX` mínimo (passthrough de bytes UTF-8); números vía `strtod`; liberado con `json_free` recursivo):

Implementación completa (~300 líneas): escritor con struct interno de contexto de comas (pila por profundidad), y parser con puntero de lectura + funciones estáticas `parse_value/parse_string/parse_number/parse_object/parse_array`, saltando whitespace, validando sintaxis estricta (coma entre elementos, cierre correcto), devolviendo NULL ante cualquier error. El objeto almacena pares (key,value) como lista enlazada de `json_value` con `key` y `next` (búsqueda por `json_obj_get` recorre la lista). Copiar el patrón de estilo de src/wav.c.

- [ ] **Step 5: Registrar en `Makefile.am` y `.gitignore`**

```make
check_PROGRAMS = tests/test_wav tests/test_session tests/test_dsp tests/test_stats tests/test_report tests/test_json
tests_test_json_SOURCES = tests/test_json.c src/json.c
tests_test_json_CFLAGS = $(AM_CFLAGS) -I$(srcdir)/src
tests_test_json_LDADD = -lpthread -lm
TESTS = tests/test_wav tests/test_session tests/test_dsp tests/test_stats tests/test_report tests/test_json
```

`.gitignore`: añade `/tests/test_json`.

```bash
autoreconf -i 2>&1 | tail -2 && ./configure 2>&1 | tail -2
make check 2>&1 | tail -12
```

Expected: 6 tests PASS.

- [ ] **Step 6: Commit**

```bash
git add src/json.h src/json.c tests/test_json.c Makefile.am .gitignore
git commit -m "Add minimal JSON module (writer + DOM parser) with tests"
```

---

### Task 2: SQLite + `watchdb.c` (relojes) + tests

**Files:** Create `src/watchdb.h`, `src/watchdb.c`, `tests/test_watchdb.c`; Modify `configure.ac`, `Makefile.am`, `.gitignore`

- [ ] **Step 1: Dependencia en `configure.ac`**

Tras `PKG_CHECK_MODULES([FFTW], [fftw3f])`:

```m4
PKG_CHECK_MODULES([SQLITE], [sqlite3])
```

(Esto añade `$(SQLITE_CFLAGS)` y `$(SQLITE_LIBS)`.)

- [ ] **Step 2: Escribir el test**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/stat.h>
#include "watchdb.h"

static int failures = 0;
#define CHECK(cond, msg) do { if(!(cond)) { fprintf(stderr, "FAIL: %s\n", msg); failures++; } } while(0)

int main(void)
{
	const char *dir = "test_watchdb_out";
	mkdir(dir, 0755);
	char dbpath[512];
	snprintf(dbpath, sizeof(dbpath), "%s/tg.db", dir);

	CHECK(watchdb_open(dbpath) == 0, "open");
	CHECK(watchdb_watch_count() == 0, "empty");

	CHECK(watchdb_add_watch("Omega 565", "Cal. 565", "rueda de balance dañada") == 0, "add w1");
	CHECK(watchdb_add_watch("Longines 280", "Cal. 280", "") == 0, "add w2");
	CHECK(watchdb_watch_count() == 2, "two watches");
	const struct watchdb_watch *w0 = watchdb_watch_at(0);
	CHECK(w0 && !strcmp(w0->name, "Omega 565") && !strcmp(w0->brand, "Omega"), "w0 fields");
	CHECK(w0->bph == 0 && w0->lift_angle == 0, "w0 defaults unset");

	CHECK(watchdb_rename_watch(0, "Omega 565 mod", "Cal. 565", "nota", 21600, 52.0) == 0, "rename");
	w0 = watchdb_watch_at(0);
	CHECK(w0->bph == 21600 && fabs(w0->lift_angle - 52.0) < 1e-9, "defaults set");

	CHECK(watchdb_close() == 0, "close");
	CHECK(watchdb_open(dbpath) == 0, "reopen");
	CHECK(watchdb_watch_count() == 2, "persisted");
	w0 = watchdb_watch_at(0);
	CHECK(!strcmp(w0->name, "Omega 565 mod") && w0->bph == 21600, "persisted fields");

	CHECK(watchdb_remove_watch(0) == 0, "remove");
	CHECK(watchdb_watch_count() == 1, "one left");
	CHECK(watchdb_watch_at(0) && !strcmp(watchdb_watch_at(0)->name, "Longines 280"), "remaining");

	watchdb_close();

	remove(dbpath);
	rmdir(dir);
	if(failures) { fprintf(stderr, "%d failure(s)\n", failures); return 1; }
	printf("watchdb tests passed\n");
	return 0;
}
```

- [ ] **Step 3: Implementar `src/watchdb.h`** (cabecera GPL-2):

```c
#ifndef TG_WATCHDB_H
#define TG_WATCHDB_H

#include <stdint.h>

struct watchdb_watch {
	int64_t id;
	char name[64];        /* obligatorio */
	char brand[64];
	char model[64];
	char serial[64];
	char year[16];
	char notes[256];
	uint64_t created_ms;
	int bph;              /* 0 = sin definir */
	double lift_angle;    /* 0 = sin definir */
};

/* Abre (y crea si falta) la base en `path`. 0 = ok. */
int watchdb_open(const char *path);
int watchdb_close(void);

int watchdb_watch_count(void);
const struct watchdb_watch *watchdb_watch_at(int i);
/* add devuelve 0 = ok. rename actualiza campos + defaults de análisis. */
int watchdb_add_watch(const char *name, const char *brand, const char *notes);
int watchdb_rename_watch(int idx, const char *name, const char *brand,
                         const char *notes, int bph, double lift_angle);
int watchdb_remove_watch(int idx);   /* borra el reloj y sus sesiones (CASCADE) */

#endif
```

- [ ] **Step 4: Implementar `src/watchdb.c`** (cabecera GPL-2):

```c
#include "watchdb.h"
#include "stats.h"     /* POSITION_* no se usa aquí; se incluye en Task 3 */
#include <sqlite3.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <glib.h>      /* g_get_real_time */

static sqlite3 *db = NULL;

int watchdb_open(const char *path)
{
	if(db) watchdb_close();
	if(sqlite3_open(path, &db) != SQLITE_OK) {
		if(db) { fprintf(stderr, "watchdb: %s\n", sqlite3_errmsg(db)); sqlite3_close(db); db = NULL; }
		return 1;
	}
	char *err = NULL;
	if(sqlite3_exec(db,
		"PRAGMA foreign_keys=ON;"
		"CREATE TABLE IF NOT EXISTS watches ("
		" id INTEGER PRIMARY KEY, name TEXT NOT NULL,"
		" brand TEXT DEFAULT '', model TEXT DEFAULT '',"
		" serial TEXT DEFAULT '', year TEXT DEFAULT '',"
		" notes TEXT DEFAULT '', created_ms INTEGER,"
		" bph INTEGER DEFAULT 0, lift_angle REAL DEFAULT 0);"
		"CREATE TABLE IF NOT EXISTS sessions ("
		" id INTEGER PRIMARY KEY, watch_id INTEGER NOT NULL REFERENCES watches(id) ON DELETE CASCADE,"
		" start_ms INTEGER, end_ms INTEGER, position INTEGER, note TEXT DEFAULT '',"
		" n INTEGER, mean REAL, sigma REAL, min REAL, max REAL,"
		" mean_be REAL, mean_amp REAL,"
		" bph INTEGER, lift_angle REAL, cal INTEGER, gain REAL, cutoff INTEGER);",
		NULL, NULL, &err) != SQLITE_OK) {
		fprintf(stderr, "watchdb: %s\n", err ? err : "?");
		sqlite3_free(err);
		sqlite3_close(db);
		db = NULL;
		return 1;
	}
	return 0;
}

int watchdb_close(void)
{
	if(!db) return 0;
	sqlite3_close(db);
	db = NULL;
	return 0;
}
```

Estado de consulta: tras `watchdb_open`, cargar la lista de relojes en memoria (array estático con mutex, patrón stats) mediante `SELECT ... ORDER BY id`; `watchdb_watch_count/watch_at` leen ese array; `add/rename/remove` ejecutan su SQL y refrescan el array. `watchdb_add_watch` inserta con `created_ms = g_get_real_time()/1000`. Límites: `WATCHDB_MAX_WATCHES 256`, campos copiados con `snprintf`/`strncpy` acotados. (Implementación completa en el módulo; ~200 líneas.)

- [ ] **Step 5: Registrar en `Makefile.am` y `.gitignore`**

En `configure.ac` (tras FFTW):
```m4
PKG_CHECK_MODULES([SQLITE], [sqlite3])
```

En `Makefile.am`, añade a `AM_CFLAGS`/`LIBS` o directamente:

```make
check_PROGRAMS = ... tests/test_watchdb
tests_test_watchdb_SOURCES = tests/test_watchdb.c src/watchdb.c src/stats.c
tests_test_watchdb_CFLAGS = $(SQLITE_CFLAGS) $(AM_CFLAGS) -I$(srcdir)/src
tests_test_watchdb_LDADD = $(SQLITE_LIBS) -lpthread -lm
TESTS = ... tests/test_watchdb
```

Y en el binario principal, añade `$(SQLITE_CFLAGS)` a `AM_CFLAGS` y `$(SQLITE_LIBS)` a `LIBS`.

`.gitignore`: añade `/tests/test_watchdb`, `/test_watchdb_out/`.

```bash
autoreconf -i 2>&1 | tail -2 && ./configure 2>&1 | tail -2
make check 2>&1 | tail -12
```

Expected: 7 tests PASS.

- [ ] **Step 6: Commit**

```bash
git add src/watchdb.h src/watchdb.c tests/test_watchdb.c configure.ac Makefile.am .gitignore
git commit -m "Add watch database module (SQLite) with tests"
```

---

### Task 3: Sesiones + captura + export JSON

**Files:** Modify `src/watchdb.h`, `src/watchdb.c`, `tests/test_watchdb.c`

- [ ] **Step 1: API de sesiones en `src/watchdb.h`**

```c
struct watchdb_session {
	int64_t id;
	int64_t watch_id;
	uint64_t start_ms, end_ms;
	int position;        /* POSITION_* */
	char note[128];
	int n;
	double mean, sigma, min, max;
	double mean_be, mean_amp;
	int bph, cal;
	double lift_angle, gain;
	int cutoff;
};

int watchdb_load_sessions(int64_t watch_id);   /* carga el índice en memoria */
int watchdb_session_count(void);
const struct watchdb_session *watchdb_session_at(int i);
/* Crea el registro: toma n/mean/sigma/min/max/mean_be/mean_amp de stats_summary(0)
 * y la config actual de los parámetros. watch_id debe existir. 0 = ok. */
int watchdb_capture_session(int64_t watch_id, uint64_t start_ms, uint64_t end_ms,
                            int position, const char *note,
                            int bph, double lift_angle, int cal, double gain, int cutoff);
int watchdb_remove_session(int64_t session_id);

/* Exporta un reloj + sus sesiones como JSON (vía json.c). 0 = ok. */
int watchdb_export_watch_json(int64_t watch_id, const char *path);
```

- [ ] **Step 2: Implementar en `src/watchdb.c`**

- `watchdb_load_sessions(watch_id)`: `SELECT ... FROM sessions WHERE watch_id = ? ORDER BY start_ms` → array en memoria (cap 4096).
- `watchdb_capture_session(...)`: llama `stats_summary(0, &s)`; INSERT con todos los campos; refresca el índice si el watch cargado coincide.
- `watchdb_remove_session(id)`: DELETE + refresco.
- `watchdb_export_watch_json(watch_id, path)`: usa `struct json_writer` (buffer dinámico: recorre `watchdb_watch_at` + sesiones cargadas; escribe `{ "watch": {...}, "sessions": [...] }`; buffer de 64 KB con `malloc`; escribe a archivo). Devuelve 0 = ok.

- [ ] **Step 3: Ampliar `tests/test_watchdb.c`**

Tras la parte de relojes (antes del close final):

```c
	/* Sesiones */
	CHECK(watchdb_load_sessions(w0->id) == 0, "load sessions");
	CHECK(watchdb_session_count() == 0, "no sessions");
	int64_t wid = watchdb_watch_at(0)->id;
	CHECK(watchdb_capture_session(wid, 1000, 2000, POSITION_DU, "prueba",
	                              21600, 52.0, 0, 0.5, 3000) == 0, "capture");
	CHECK(watchdb_session_count() == 1, "one session");
	const struct watchdb_session *ss = watchdb_session_at(0);
	CHECK(ss->position == POSITION_DU && ss->bph == 21600 && fabs(ss->gain - 0.5) < 1e-9, "session fields");
	CHECK(watchdb_capture_session(wid, 3000, 4000, POSITION_DD, "", 21600, 52.0, 0, 0.5, 3000) == 0, "capture 2");
	CHECK(watchdb_session_count() == 2, "two sessions");

	/* Export JSON */
	char jpath[512];
	snprintf(jpath, sizeof(jpath), "%s/export.json", dir);
	CHECK(watchdb_export_watch_json(wid, jpath) == 0, "export ok");
	{
		FILE *f = fopen(jpath, "r");
		CHECK(f != NULL, "json exists");
		if(f) {
			char buf[8192] = {0};
			size_t r = fread(buf, 1, sizeof(buf)-1, f);
			buf[r] = 0;
			fclose(f);
			CHECK(strstr(buf, "\"sessions\"") != NULL, "json sessions");
			CHECK(strstr(buf, "dial up") != NULL, "json position");
		}
	}
	remove(jpath);
	CHECK(watchdb_remove_session(watchdb_session_at(0)->id) == 0, "remove session");
	CHECK(watchdb_session_count() == 1, "one session left");
```

(Nota: los valores de mean/sigma del session capturado dependen del anillo de stats — el test solo verifica campos de config y conteos. Para `stats_summary` el anillo está vacío en el test → n=0; `watchdb_capture_session` debe funcionar igualmente con n=0 (guard).)

- [ ] **Step 4: Verificar y commit**

```bash
make check 2>&1 | tail -8
git add src/watchdb.h src/watchdb.c tests/test_watchdb.c
git commit -m "Add session records, capture and JSON export to watch database"
```

---

### Task 4: Docs + verificación + merge

**Files:** Modify `docs/ROADMAP.md`, `README.md`, `docs/DEVELOPMENT.md`

- [ ] **Step 1: `docs/ROADMAP.md` — Fase 5.5a completada**

```markdown
## Fase 5.5a — Registro de relojes (watch-db) (completada)

**Rama:** `feature/watch-db` — **Estado:** completada.

- Base SQLite en `~/tg-data/tg.db`: tablas `watches` y `sessions` (con
  snapshot de configuración por sesión).
- CRUD completo de relojes y sesiones (`watchdb.c`) + captura de sesión
  (`watchdb_capture_session`) + export JSON por reloj (`json.c`).
- Pendiente (5.5b): panel izquierdo UI, ciclo de sesión y gráfico de evolución.
```

- [ ] **Step 2: `README.md` (EN) y `README.es.md`**

En Install/Compiling: añadir la dependencia SQLite (`libsqlite3-dev` Debian, `sqlite-devel` Fedora, `mingw-w64-x86_64-sqlite3` msys2). En Features: añadir "Watch database: persistent watch registry and session history (SQLite, `~/tg-data/tg.db`)".

- [ ] **Step 3: `docs/DEVELOPMENT.md`** — añadir al mapa de módulos:

```markdown
| `json.c` | Writer + parser JSON mínimo | — |
| `watchdb.c` | Registro de relojes y sesiones (SQLite) | SQLite, stats |
```

- [ ] **Step 4: Verificación completa + commit + push + merge**

```bash
make check 2>&1 | tail -6
make tg-timer 2>&1 | grep -iE "warning|error"; echo "no warnings"
git add -A
git commit -m "Document watch database"
git push origin feature/watch-db
git checkout master && git merge feature/watch-db && git push origin master
git branch -d feature/watch-db
git push origin --delete feature/watch-db
```

---

## Self-review

- **Cobertura:** json → Task 1; SQLite+relojes → Task 2; sesiones+captura+export → Task 3; docs/merge → Task 4.
- **Dependencia nueva:** libsqlite3-dev documentada en README e integrada por PKG_CHECK_MODULES.
- **Sin placeholders:** el código de Task 1 y Task 2 Steps 1-4 está completo; Task 2 Step 4 (implementación de consulta/estado) y Task 3 describen el patrón (mutex + array estático + SQL preparado) siguiendo el estilo existente del repo.
- **Seguridad de datos:** guardado atómico por SQLite (transacciones), esquema con FK CASCADE, apertura idempotente (CREATE IF NOT EXISTS).