# Fase 7a — Port de fixes algorítmicos de xyzzy42 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Portar a nuestro fork los fixes algorítmicos de mayor valor de xyzzy42/tg (Trent Piepho, v0.7.0): soporte de BPH bajos (8100+, elimina nuestra limitación documentada de 12000/14400), sigma de 1 muestra = 0, y correcciones menores (conjf, alocación FFTW).

**Architecture:** Cambios quirúrgicos en `src/algo.c` y `src/tg.h` portando la lógica exacta de xyzzy42/tg (commits e1e4367, 5fa7289, cbd3a00, 774613f, 675507c, verificados contra /tmp/opencode/xyzzy42-tg), con **atribución explícita a Trent Piepho** en los commits. El test de regresión DSP se actualiza: 12000/14400 BPH pasan de `signal=0` (limitación) a `signal=1` (soportados).

**Tech Stack:** C99, FFTW, autotools `make check`.

**Rama:** `feature/xyzzy42-algo` (creada desde `master`).

---

### Task 1: Fix BPH bajos

**Files:** Modify `src/algo.c`, `src/tg.h`, `tests/test_dsp.c`

- [ ] `src/algo.c` `compute_period`: sustituir el guard fijo por el límite dinámico:

```c
	/* Limit to 20% greater when period is known, or 500 ms when guessing period */
	const int min_bph = bph ? bph : TYP_BPH;
	const int max_period = (int)(1.2 * 3600 * 2) * p->sample_rate / min_bph;
	if(p->ready && p->period >= max_period) {
```

- [ ] `src/tg.h`: `#define MIN_BPH 12000` → `#define MIN_BPH 8100` + añadir `#define TYP_BPH 12000`.
- [ ] `tests/test_dsp.c`: en el array `cases[]`, `{ 12000, 0 }` → `{ 12000, 1 }` y `{ 14400, 0 }` → `{ 14400, 1 }`; actualizar el comentario (la limitación ya no existe).
- [ ] Verificar: `make check` (7 PASS con el flip), `make tg-timer` sin warnings.

### Task 2: Sigma de 1 muestra = 0

- [ ] `src/algo.c` `compute_period` final: `b->sigma = b->period;` → `b->sigma = 0;	/* No std. dev. estimate possible with just 1 sample */`

### Task 3: conjf + alocación FFTW

- [ ] `src/algo.c`: `conj(` → `conjf(` en los 3 sitios (sc_fft/slice_fft, double→float).
- [ ] `src/algo.c` `setup_buffers`: `malloc` → `fftwf_malloc` en `samples_sc`, `waveform`, `waveform_sc`; y `pb_destroy`: `free` → `fftwf_free` en esos 3 (coherencia de alocación FFTW).

### Task 4: Verificación + docs + merge

- [ ] `make check` 7/7; build sin warnings; smoke; `--analyze` sintético OK.
- [ ] `docs/ROADMAP.md`: Fase 7a completada + eliminar la "limitación conocida" de BPH bajos.
- [ ] `docs/USAGE.md`/`USAGE.en.md`: rango BPH soportado 8100+.
- [ ] Commit(s) con atribución ("Port from xyzzy42/tg by Trent Piepho, commit <sha>") → push → merge → borrar rama.

## Attribution

Todos los cambios provienen de [xyzzy42/tg](https://github.com/xyzzy42/tg) por **Trent Piepho** (GPL-2), commits e1e4367, 5fa7289, cbd3a00, 774613f, 675507c.